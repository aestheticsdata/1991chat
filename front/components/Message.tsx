// Message — a single chat bubble (user / assistant), including the pending
// ("thinking") and error states the backend reports.
// TODO (you): props + rendering.
export function Message() {
  return null;
}
