// MessageList — map your messages to <Message /> items.
// TODO (you): messages.map((m) => <Message key={m.id} ... />)
export function MessageList() {
  return <div className="space-y-4">{/* TODO: render messages */}</div>;
}
