node_modules
dist
*.log
*.tsbuildinfo

# Local environment
.env

# SQLite database files (data on disk)
/data
*.db
*.db-shm
*.db-wal

# Local mock corpus — large gitignored artifact, built once, kept out of the repo
/assets/mock-corpus.txt
